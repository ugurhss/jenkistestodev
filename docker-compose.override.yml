version: '3.8'

services:
  app:
    volumes:
      # Keep vendor and node_modules in docker-managed volumes for local dev
      - vendor:/app/vendor
      - node_modules:/app/node_modules
    ports:
      - "8000:8000"
  db:
    ports:
      - "3307:3306"

volumes:
  vendor:
  node_modules:
